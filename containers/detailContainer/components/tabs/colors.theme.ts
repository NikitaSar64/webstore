import { createTheme } from "@mui/material/styles";

export const colorsTheme = createTheme({
  palette: {
    primary: {
      main: "#263238",
    },
    secondary: {
      main: "#8bc34a",
    },
  },
});
